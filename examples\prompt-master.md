# Prompt Master Example

```text
Use Prompt Master. Create a prompt for Codex that safely refactors the auth module.
Context: Node.js API with tests.
Must return: implementation plan, patch, tests run.
Must not: add dependencies, touch production, edit unrelated files.
```
